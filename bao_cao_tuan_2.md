# BÁO CÁO CÔNG VIỆC CÁ NHÂN - TUẦN 2
**Dự án:** ShopFlow E-commerce
**Giai đoạn:** 18/05/2026 - 24/05/2026

| Công việc | Chi tiết | Ngày | Trạng thái | Tiến triển | Đầu ra |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Prisma schema & PostgreSQL** | Thiết kế model, relation, enum và kết nối tới database PostgreSQL thật. | 18/05/2026 | Hoàn thành | Đã thiết kế các Model (User, Product, Order, Cart...) và Enums. Cấu hình .env trỏ tới PostgreSQL cục bộ và chạy lệnh prisma migrate dev để đồng bộ. | File schema.prisma, cấu trúc thư mục prisma/migrations và Database chứa các bảng thực tế. |
| **Prisma Client (Type-safe Query)** | Tích hợp PrismaClient vào NestJS và áp dụng Repository/Service Pattern. | 19/05/2026 | Hoàn thành | Đã tạo PrismaService và PrismaModule (cấu hình Global). Tiến hành refactor thành công ProductService từ lưu mảng RAM tạm sang các lệnh truy vấn database thực tế (type-safe). Chạy lệnh prisma generate và compile code không lỗi. | PrismaModule sẵn sàng dùng chung cho toàn dự án. ProductService đã tương tác trực tiếp được với DB thật. |
| **Prisma Seed Data** | Chèn dữ liệu mẫu cho môi trường dev. | 20/05/2026 | Hoàn thành | Đã viết file prisma/seed.ts chèn các Role, User, Category và Product mẫu. Xử lý thành công việc tương thích Driver Adapter của Prisma 7. | Lệnh `npx prisma db seed` chạy thành công và đổ dữ liệu chuẩn vào DB. |
| **Password Hashing (bcrypt)** | Băm mật khẩu người dùng trước khi lưu vào database. | 20/05/2026 | Hoàn thành | Đã cài đặt thư viện bcrypt. Triển khai mã hóa một chiều (10 salt rounds) trong hàm khởi tạo User của UserService và áp dụng cho toàn bộ dữ liệu mẫu trong seed.ts. | Mật khẩu trong DB hiện tại đã được mã hóa an toàn. |
| **Passport JWT Strategy** | Tích hợp JWT để cấp phát Token (Đăng nhập) và bảo vệ API (Xác thực). | 21/05/2026 | Hoàn thành | Đã cài bộ công cụ @nestjs/jwt, passport-jwt. Viết JwtStrategy và JwtAuthGuard. Tạo luồng login hoàn chỉnh (từ so sánh hash password tới ký sinh token) trong AuthController. | API /auth/login trả về token hợp lệ. Route /auth/profile được bảo vệ chặt chẽ bởi JwtAuthGuard. |
| **Đồng bộ Schema Thiết kế** | Cập nhật cấu trúc DB cho User (status), Inventory (reserved) và AuditLog. | 22/05/2026 | Hoàn thành | Đã thêm UserStatus, tách bảng Inventory, sửa tên cột AuditLog. Chạy db push thành công và cập nhật lại toàn bộ seed.ts. | Database hoàn toàn khớp với hình ảnh thiết kế thực tế. Bảng Inventory đã sẵn sàng. |
| **Phân quyền Roles Guard** | Phân quyền truy cập dựa trên vai trò admin/staff/customer. | 22/05/2026 | Hoàn thành | Đã tạo RolesGuard và custom decorator @Roles để tự động kiểm tra role (trích xuất từ JWT) của request gửi lên. Tự động chặn các truy cập trái phép. | File roles.guard.ts và roles.decorator.ts trong thư mục src/auth. |
| **Admin Product API** | Quản lý thao tác dữ liệu product tuân thủ nghiêm ngặt theo role. | 23/05/2026 | Hoàn thành | Đã áp dụng Guard vào API. Admin/Staff được quyền thêm/sửa sản phẩm. Quyền xóa sản phẩm được giới hạn tuyệt đối chỉ dành cho Admin. Khách hàng chỉ được quyền xem. | File product.controller.ts với các API Create, Update, Delete đã được khóa Guard. |
| **Swagger protected docs** | Cấu hình giao diện Swagger để sử dụng được với Bearer token (JWT). | 23/05/2026 | Hoàn thành | Đã thiết lập hàm .addBearerAuth() lúc khởi tạo Swagger và gắn cờ @ApiBearerAuth() lên toàn bộ các endpoint cần bảo mật trong hệ thống. | Giao diện tài liệu Swagger UI hiện có nút "Authorize" để test API bằng Token thật. |
| **Frontend Authentication** | Xây dựng giao diện và tích hợp API Đăng nhập/Đăng ký cho người dùng. | 24/05/2026 | Hoàn thành | Xây dựng form Đăng nhập/Đăng ký. Cấu hình AuthContext quản lý Global State qua localStorage. Gắn JWT Bearer tự động vào mọi request qua Axios Interceptor. | Các file AuthContext.tsx, Login.tsx, Register.tsx hoàn thiện. Khách hàng có thể đăng ký và đăng nhập thành công. |
| **Backend DB Sync (Prisma)** | Xử lý lỗi Database Schema out-of-sync khiến API Đăng ký bị lỗi 500. | 24/05/2026 | Hoàn thành | Khắc phục lỗi thiếu cột status trong Database do Drift. Chạy lại prisma migrate dev để đồng bộ bảng User và prisma db seed để tạo Roles mặc định. | Lỗi Invalid Prisma invocation đã được sửa triệt để tại auth.service.ts. Luồng tạo tài khoản hoạt động trơn tru. |
| **User Profile & Navbar UI** | Hiển thị thông tin cá nhân (Họ Tên) trên Navbar sau khi đăng nhập thành công. | 24/05/2026 | Hoàn thành | Cập nhật API GET /auth/profile lấy đầy đủ firstName, lastName từ DB dựa trên userId trong Token. Xử lý logic ghép chuỗi Họ Tên và hiển thị nút Đăng xuất. | File ClientLayout.tsx hiển thị đúng "Xin chào, [Họ và Tên]". Nút Đăng xuất hoạt động (clear storage & redirect). |
